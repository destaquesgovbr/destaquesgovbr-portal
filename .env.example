# Typesense Configuration
# Get the IP from the GCP Compute Engine console or Terraform output
NEXT_PUBLIC_TYPESENSE_HOST=34.39.186.38

# Search-only API key (read-only, safe to expose in client)
# This should be different from the admin API key used for indexing
NEXT_PUBLIC_TYPESENSE_SEARCH_ONLY_API_KEY=your_search_only_api_key_here

# Site URL (used for SEO, Open Graph, etc.)
# Development:
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Production:
# NEXT_PUBLIC_SITE_URL=https://destaquesgovbr.com.br
NEXT_PUBLIC_SITE_URL=http://localhost:3000
